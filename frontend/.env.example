# Example frontend environment variables
# For production set this to your production API base (include the /api path if needed)
# VITE_API_URL=https://your-backend.example.com/api

# For local development, leave commented to use Vite proxy (default '/api')
# or set to your backend URL
# VITE_API_URL=http://localhost:3000
